import {

} from '../actions/types'

const initialState = {
    isLoading: false,
    user: null,

}

export default function (state = initialState, action) {
    switch (action.type) {


        default:
            return state;
    }
}

